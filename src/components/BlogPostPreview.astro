---
export interface Props {
  post: any;
}

const { post } = Astro.props;
---

<div class="mb-12">
  <h3 class="text-md text-slate-10">{post.frontmatter.publishDate}</h3>
  <a class="text-blue-12" href={`${post.url}/`}>
    <h2 class="text-xl underline">{post.frontmatter.title}</h2>
  </a>
</div>
